# Peigen - Environment Variables Example
# Copy this file to .env.local and customize the values as needed

# API Request Configuration
# Timeout for API requests in milliseconds (default: 30000)
REACT_APP_DEFAULT_TIMEOUT=30000

# Maximum number of requests to store in history (default: 50)
REACT_APP_MAX_HISTORY_SIZE=50

# Local Storage Configuration
# Key prefix for localStorage items (default: peigen_data)
REACT_APP_LOCAL_STORAGE_KEY=peigen_data

# Application Information
# Application version (default: 1.0.0)
REACT_APP_VERSION=1.0.0

# Application name (default: Peigen)
REACT_APP_NAME=Peigen
